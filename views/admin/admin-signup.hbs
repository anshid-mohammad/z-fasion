

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>signup Form</title>
  <!-- Bootstrap 5 CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons CDN -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
  <link rel='stylesheet' href='/stylesheets/login.css' />
  <style>
    .main-text{
    color: #7c540e;

font-size: 4rem;
}
.subtext-login{
   font-size: 17px;
   color: #7c540e;
}
.form-control{
 
   
    width: 100%;
    padding: 12px;
    border-radius: 25px;
    border: 1px solid #e0e0e0;
    outline: none;
    box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
    font-size: 16px;
    transition: all 0.2s ease;
   

}
.d-flex{
    background-color: aqua;
    height: 20px;
    width: 12rem;
  
}

.btn-login{
   
   
    height: 3rem;
           margin-top: 2rem;
        background-color: #c58e58; /* Bootstrap primary color */
        color: white; /* Text color */
        border: none; /* No border */
        border-radius: 30px; /* Rounded corners */
        padding: 10px 20px; /* Padding for size */
        font-size: 1rem; /* Base font size */
        transition: background-color 0.3s, transform 0.2s; /* Smooth transitions */
        /* Full width *
        margin-left: 8rem;
        cursor: pointer;
    }
    @media (min-width: 768px) {  Medium devices (md) */
}


    .btn-login:hover{
        background-color: #7a5e03; /* Darker on hover */
        transform: scale(1.05); /* Slight zoom effect */
    }

    .btn-login:active{
        transform: scale(0.95); /* Slight shrink on click */
    }
    .new-account{
        margin-left: 10rem;
        margin-top: 2rem;
        float: left;
        color: #7c540e;
    }
    .signup-link{
        margin-top: 30px;
        float: left;
        color: #0b0b0b;
        font-weight: bold;
        
    }
    .email-color{
        color: #7c540e;

    }





    .password-toggle {
      cursor: pointer;
    }
    /* Center the login button */
    .login-btn-container {
      text-align: center;
    }
    /* Add margin-top to move the link down */
    .signup-link {
     
      display: block;
      text-align: center;
    }
  </style>
</head>
<body>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-8 col-sm-10 col-12">
            <h1 class="main-text text-center">Signup</h1>
            <h4 class="text-center subtext-login">Hi! Welcome back, You’ve Been Missed</h4>

            <form action="/admin/admin-signup" method="post" class="mt-4">
             <div class="form-group mb-3">
                    <label class="email-color" for="name">Name</label>
                    <input type="text" class="form-control" id="name" name="name" placeholder="Enter name" required>
                </div>
                {{!-- <!-- Error Message -->
                {{#if loginErr}}
                <p class="error text-danger">{{loginErr}}</p>
                {{/if}} --}}

                <!-- Email Input -->
                <div class="form-group mb-3">
                    <label class="email-color" for="email">Email address</label>
                    <input type="email" class="form-control" id="email" name="Email" placeholder="Enter email" required>
                </div>

                <!-- Password Input with Show/Hide Feature -->
                <div class="form-group mb-3">
                    <label class="email-color" for="password">Password</label>
                    <div class="input-group">
                        <input type="password" class="form-control" id="password" name="password" placeholder="Enter password" required>
                        <span class="input-group-text password-toggle" onclick="togglePassword()">
                            <i class="bi bi-eye" id="toggleIcon"></i> <!-- Bootstrap icon for eye -->
                        </span>
                    </div>
                </div>

                <!-- Centered Login Button -->
                <div class="login-btn-container">
                    <button type="submit" class="btn-login btn-primary" style="width: 12rem;">Signup</button>
                </div>

                <!-- Sign Up Link below the button -->
                <p class="new-account">Allready have account? </p>
                <a href="/admin/admin-login" class="signup-link">login</a>
            </form>
        </div>
    </div>
</div>

<!-- Bootstrap 5 JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<!-- Bootstrap Icons CDN -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.js"></script>
<script>
  function togglePassword() {
    var passwordInput = document.getElementById("password");
    var toggleIcon = document.getElementById("toggleIcon");
    if (passwordInput.type === "password") {
      passwordInput.type = "text";
      toggleIcon.classList.remove("bi-eye");
      toggleIcon.classList.add("bi-eye-slash");
    } else {
      passwordInput.type = "password";
      toggleIcon.classList.remove("bi-eye-slash");
      toggleIcon.classList.add("bi-eye");
    }
  }
</script>
</body>
</html>
