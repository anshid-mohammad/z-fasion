<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh; /* Full viewport height */
            display: flex;
            justify-content: center; /* Center content horizontally */
            align-items: center; /* Center content vertically */
            background-image: url('https://images.unsplash.com/photo-1506748686214-e9df14d4d9d0'); /* Background image */
            background-size: cover; /* Cover entire background */
            background-position: center; /* Center background image */
            color: #fff; /* White text for contrast */
            font-family: 'Arial', sans-serif;
        }

        .profile-container {
            background: rgba(0, 0, 0, 0.7); /* Semi-transparent background for readability */
            border-radius: 15px;
            padding: 30px;
            max-width: 500px; /* Maximum width */
            width: 100%; /* Full width on small screens */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* Shadow effect */
            text-align: center; /* Center text */
        }

        .profile-container h2 {
            margin-bottom: 20px;
            font-size: 2rem; /* Title size */
        }

        .profile-img {
            border-radius: 50%; /* Circular image */
            width: 120px; /* Profile image size */
            height: 120px; /* Profile image size */
            object-fit: cover; /* Cover aspect */
            margin-bottom: 15px; /* Space below image */
            border: 4px solid #d5b87d; /* Border around image */
        }

        .user-details {
            margin-bottom: 20px; /* Space below details */
            font-size: 1.1rem; /* Size of user details */
        }

        .profile-actions {
            display: flex;
            justify-content: space-around; /* Space between buttons */
            margin-top: 20px; /* Space above buttons */
        }

        .action-button {
            padding: 10px 20px;
            background-color: #d5b87d; /* Button background color */
            color: #fff; /* Button text color */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners */
            text-decoration: none; /* No underline */
            transition: background-color 0.3s, transform 0.3s; /* Smooth transition */
            flex: 1; /* Equal space for buttons */
            margin: 0 5px; /* Space between buttons */
        }

        .action-button:hover {
            background-color: #936e00; /* Darken on hover */
            transform: scale(1.05); /* Slightly enlarge */
        }

        @media (max-width: 768px) {
            .profile-container {
                padding: 20px; /* Less padding on small screens */
            }

            .profile-img {
                width: 100px; /* Smaller image size on small screens */
                height: 100px; /* Smaller image size on small screens */
            }

            .profile-container h2 {
                font-size: 1.5rem; /* Smaller title on small screens */
            }

            .user-details {
                font-size: 1rem; /* Smaller text on small screens */
            }
        }
    </style>
</head>

<body>
    <div class="profile-container">
        <h2 style="font-family: initial;font-weight: bold;">{{user.name}}</h2>
        <div class="user-details">
            <p><strong>Email:</strong> {{user.Email}}</p>
            <p><strong>Phone:</strong> {{user.Mobile}}</p>
            <p><strong>Address:</strong> {{user.address}}</p>
        </div>
        <div class="profile-actions">
            <a href="/orders" class="action-button">View Orders</a>
            <a href="/logout" class="action-button">Logout</a>
          <a href="/" class="action-button">Home</a>

        </div>
    </div>
</body>
</html>
