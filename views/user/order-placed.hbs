
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful</title>
    <style>
        /* Body & Container Styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            
            background-color: #f8ebd4;
        
        }

        .container {
            text-align: center;
            background-color: #ffffff;
            border-radius: 15px;
            padding: 50px 40px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        /* Checkmark Circle */
        .checkmark-circle {
            position: relative;
            width: 100px;
            height: 100px;
            background-color:  #936e00;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0 auto 20px;
            animation: pop 0.3s ease-out forwards;
        }

        /* Checkmark */
        .checkmark {
            width: 50px;
            height: 25px;
            border-radius: 5px;
            border-width: 5px;
            border-style: solid;
            border-color: #fff;
            border-top: none;
            border-right: none;
            transform: rotate(-45deg);
            animation: draw 0.5s ease-in-out forwards 0.4s;
        }

        /* Animations */
        @keyframes pop {
            from { transform: scale(0); }
            to { transform: scale(1); }
        }

        @keyframes draw {
            from { stroke-dashoffset: 50px; }
            to { stroke-dashoffset: 0; }
        }

        /* Success Message */
        .message {
            font-weight: bold;
            font-family: initial;
            font-size: 24px;
            font-weight: bold;
            color: #333;
        }

        .subtext {
            font-weight: bold;
            font-family: initial;
            font-size: 16px;
            color: #666;
            margin-top: 10px;
        }

        /* Buttons */
        .button-container {
            margin-top: 20px;
        }

        .button {
            display: inline-block;
            margin: 10px 5px;
            padding: 12px 24px;
            font-size: 16px;
            color: #fff;
            background-color: #e0b943;;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .button:hover {
            background-color: #45a049;
        }
         a {
        text-decoration: none;
        color: inherit;
    }
    a:link, a:visited {
    text-decoration: none; /* No underline on normal or visited links */
    color: inherit; /* Inherit color from button */
}

    </style>
</head>
<body>

<div class="container">
    <div class="checkmark-circle">
        <div class="checkmark"></div>
    </div>
    <div class="message">Payment Successful!</div>
    <p class="subtext">Thank you for your purchase.</p>

    <!-- Buttons -->
    <div class="button-container">
        <a href="/orders" style="font-family: initial; font-weight: bolder;" class="button">View Order</a>
        <a href="/" style="font-family: initial; font-weight: bolder;" class="button">Go to Home</a>
    </div>
</div>

</body>
</html>
