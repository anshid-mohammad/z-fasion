{{!-- {{!-- <div class="form-wrapper">
    <form id="loginform">
        <div class="input-field">
            <label>phone Number</label>
            <input type="text" placeholder="Phone" name="phone">
        </div>
        <button class="main-button" type="submit" id="sign-in-button">Sign in</button>
    </form>
</div>

<div class="form-wrapper">
    <form id="otp-form">
        <div class="input-field">
            <label>Enter OTP</label>
            <input type="number" placeholder="One time password" name="otp-value">
        </div>
        <button class="main-button" type="submit">Verify OTP</button>
    </form>
</div> --}}



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Phone Authentication</title>
    <link rel="stylesheet" href="otp-demo.css">

    <!-- Firebase App (the core Firebase SDK) -->
    <script src="https://www.gstatic.com/firebasejs/9.15.0/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.15.0/firebase-auth.js"></script>
     <script src="/config/firebase.js"></script>
</head>

<body>
    <style>
       * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #ffeb86;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #f4f7f6;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.form-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 400px;
    padding: 20px;
    background-color: white;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
}

.form-wrapper {
    width: 100%;
    margin-bottom: 20px;
    

}

.input-field {
    margin-bottom: 15px;
    display: flex;
    flex-direction: column;
}

.input-field label {
    font-size: 14px;
    margin-bottom: 5px;
    color: #333;
}

.input-field input {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
}

.main-button {
    width: 100%;
    padding: 12px;
    background-color:  #936e00;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
    transition: background-color 0.3s ease;
}

.main-button:hover {
    background-color:  #936e00;
}

input:focus {
    outline: none;
    border-color: #936e00;
    box-shadow: 0 0 5px rgba(128, 189, 255, 0.5);
}

.form-wrapper {
    width: 100%;
    margin-bottom: 20px;
}

.input-field {
    margin-bottom: 15px;
    display: flex;
    flex-direction: column;
}

.input-field label {
    font-size: 14px;
    margin-bottom: 5px;
    color: #936e00;
}

.input-field input {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
}

.main-button {
    width: 100%;
    padding: 12px;
    background-color: #936e00;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
    transition: background-color 0.3s ease;
}

.main-button:hover {
    background-color: #218838;
}

input:focus {
    outline: none;
    border-color: #936e00;
    box-shadow: 0 0 5px rgba(128, 189, 255, 0.5);
}
#otp-form{
    display: none;
}


 .text-otp{
       margin-bottom: 3rem;
    font-family: emoji;
    color: #936e00 ;
 }
    </style>

   
    
 
    <div class="form-container " id="recaptcha-container">
        <h1 class="text-otp">verify Your Number</h1>
        <!-- Phone Number Input Form -->
        <div class="form-wrapper">
            <form action="/otp-demo" id="loginform">
                <div class="input-field">
                    <label for="phone">Phone Number</label>
                    <input type="text" id="phone" placeholder="Phone" name="phone" autocomplete="off">
                </div>
                <div class="recaptcha-container">

                </div>
                <button class="main-button" type="submit" id="sign-in-button">Sign in</button>
            </form>
        </div>

        <!-- OTP Verification Form -->
        <div class="form-wrapper">
            <form id="otp-form">
                <div class="input-field">
                    <label for="otp">Enter OTP</label>
                    <input type="number" id="otp" placeholder="One time password" name="otp-value" autocomplete="off">
                </div>
                <button class="main-button" type="submit">Verify OTP</button>
            </form>
        </div>
    </div>

    <!-- Firebase Script for Authentication (Add your Firebase configuration) -->
    <script>
      const firebaseConfig = {
        apiKey: "YOUR_API_KEY",
        authDomain: "YOUR_AUTH_DOMAIN",
        projectId: "YOUR_PROJECT_ID",
        storageBucket: "YOUR_STORAGE_BUCKET",
        messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
        appId: "YOUR_APP_ID"
      };

      const app = firebase.initializeApp(firebaseConfig);
      const auth = firebase.auth();

      const sendOtp = () => {
        const phoneNumber = document.querySelector("input[name='phone']").value;
        const recaptchaVerifier = new firebase.auth.RecaptchaVerifier('sign-in-button', {
          'size': 'invisible',
          'callback': (response) => console.log('reCAPTCHA verified.')
        });

        auth.signInWithPhoneNumber(phoneNumber, recaptchaVerifier)
          .then((confirmationResult) => {
            window.confirmationResult = confirmationResult;
            alert('OTP sent. Check your phone.');
          }).catch((error) => {
            console.error(error);
            alert(error.message);
          });
      };

      const verifyOtp = () => {
        const otpValue = document.querySelector("input[name='otp-value']").value;
        window.confirmationResult.confirm(otpValue)
          .then((result) => {
            alert('OTP verified. You are signed in.');
          }).catch((error) => {
            console.error(error);
            alert('OTP verification failed.');
          });
      };

      document.getElementById('loginform').addEventListener('submit', (e) => {
        e.preventDefault();
        sendOtp();
      });

      document.getElementById('otp-form').addEventListener('submit', (e) => {
        e.preventDefault();
        verifyOtp();
      });
    </script>
</body>
</html>
 --}}
